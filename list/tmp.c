#include <stdio.h>
#include <stdlib.h>

// A linked list node
struct Node
{
  int data;
  struct Node *next;
};

int main() {
        struct Node **head = NULL:
        insert(&head)
        return 0;
}
