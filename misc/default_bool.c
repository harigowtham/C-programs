#include<stdio.h>
#include <stdbool.h>

void main () {
        bool a;
        if (a) {
        printf("default is true");
        } else {
        printf("default is false");
        }

}

//default is false
