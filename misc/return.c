#include<stdio.h>
#include<string.h>
main () {
        int i = -1;
        if (fn ())
                printf("prints");
        else
                printf("doen't");
        if (!strcmp("ier","ier"))
                        printf("works");
        printf("%d", !i);
}
